/**
 * Shared constants for the Invocation editor bundle.
 */

// The single source of truth for the Invocation icon, used everywhere it
// appears (sidebar, toolbar, menu items) so it stays consistent.
export const INVOCATION_ICON = 'layout';
