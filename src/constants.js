/**
 * Shared constants for the Blocksmith editor bundle.
 */

// The single source of truth for the Blocksmith icon, used everywhere it
// appears (sidebar, toolbar, menu items) so it stays consistent.
export const BLOCKSMITH_ICON = 'layout';
